var router = require('express').Router();
var db = require('');

router.get('/playerslist', function(req, res) {
    console.log('something');
});
router.post('/submit-player');

module.exports = router;